node_modules
*.log*
.nuxt
.nitro
.cache
.output
.env
dist
.DS_Store

# Yarn
.yarn/cache
.yarn/*state*

# Local History
.history

hack/

# Claude Code: keep shared automations (skills, hooks, agents, scripts, project settings),
# ignore user-local settings and the worktree scratch dir.
.claude/settings.local.json
.claude/worktrees/
